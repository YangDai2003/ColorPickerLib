<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CustomColorPicker" parent="Theme.AppCompat.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CustomColorPicker" parent="Base.Theme.CustomColorPicker" />

    <style name="CustomDialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:windowBackground">@drawable/dialog_rounded_corners</item>
    </style>

</resources>